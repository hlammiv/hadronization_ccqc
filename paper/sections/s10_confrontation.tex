\section{Confrontation: classical cartoon, quantum framework, phenomenology}
\label{sec:confrontation}

Three independent handles now aim at the same ratios:

\begin{enumerate}
\item the classical simulation of Secs.~\ref{sec:model}--%
\ref{sec:classical-results} (cheap, dynamical, schematic color);
\item the Tier-1--4 lattice observables of Sec.~\ref{sec:hierarchy}
(rigorous, awaiting hardware at scale);
\item phenomenological baselines: statistical hadronization at
$T_{\rm ch}\approx156\MeV$ \cite{Andronic:2017pug}, coalescence
\cite{Fries:2008hs}, and the ExHIC exotic-yield systematics
\cite{ExHIC:2010gcb,ExHIC:2017smd,Zhang:2020dwn}, against data anchors such as
$\chic$ production in pp and Pb--Pb \cite{CMS:2021znk,LHCb:2021auc} and
the $\Tcc$ discovery \cite{LHCb:2021vvq}.
\end{enumerate}

\subsection{Beyond heavy ions: pp and \texorpdfstring{$p\bar p$}{ppbar}}

Nothing in either half is intrinsically heavy-ion.  The classical model
assumes only a deconfined quark ensemble that expands and hadronizes;
for minimum-bias pp that assumption competes with vacuum fragmentation,
but the small-systems phenomenology (continuity of strangeness and
baryon ratios in multiplicity across pp, $p$Pb, and PbPb) indicates
medium-like hadronization turns on already in high-multiplicity pp.
Three structural points favor the small-system application.  First, the
model is \emph{canonical by construction}: color, flavor, and baryon
number are conserved exactly event by event --- precisely the
correction grand-canonical statistical hadronization needs in small
systems.  Second, our simulated sizes ($N=10^2$--$3\times10^3$ quarks)
span high-multiplicity pp through peripheral AA.  Third, midrapidity at
collider energies is net-baryon-free for pp, $p\bar p$, and AA alike,
matching the $B=0$ initialization, so Tevatron prompt-$\chic$
production data are legitimate anchors alongside LHC pp.

\begin{figure}[t]
\includegraphics[width=\columnwidth]{figs/fig10_smallsys.pdf}
\caption{Yield ratios to mesons versus system size at fixed density,
temperature, and $H_0=0.1\,c/\fm$ (160 events at $N=96$, 32 at
$N=300$, 96 at $N=3000$).  Baryon-to-meson is size-independent above
$N\approx300$ ($1.40\pm0.36\%$, $1.41\pm0.06\%$) with a mild deficit
at $N=96$ ($1.11\pm0.25\%$); tetraquark-to-meson shows a comparable
small-size deficit ($0.19\pm0.14\%$ vs $0.34\pm0.04\%$).}
\label{fig:smallsys}
\end{figure}

A size scan (Fig.~\ref{fig:smallsys}) finds composite-to-meson ratios
size-independent above $N\approx300$, with mild
($\sim1.5\sigma$-level) deficits in the smallest droplets --- an
initial 32-event ensemble suggested a stronger suppression that
$5\times$ the statistics revised away, and we quote the honest result:
canonical-style suppression at $N\sim10^2$ is present but small in
this model, a prediction for how composite-to-meson ratios should
scale with event multiplicity (the observable LHCb has begun to
measure for $\chic$).

The crosswalks are explicit by construction.  The classical
manifest-exotic tally ($cc\bar u\bar d$ versus $c\bar c q\bar q$) is the
same partition Tier~1 draws with conserved charges.  The classical binding
criterion maps onto Tier-4 flowed projectors, with
$r_0 \leftrightarrow \sqrt{8t_{\rm fl}}$ as the scheme dial; comparing the
$\tau_{\rm bound}$-scanned classical ratios with $t_f$-scanned Tier-1--3
counts separates formation from survival in both frameworks.

Table~\ref{tab:ratios} assembles the quantitative confrontation.  The
baryon-to-meson comparison deserves an honest accounting rather than a
victory lap.  At the baseline point our total ratio is
$(B{+}\bar B)/M = 2.9\%$, against $p/\pi^+\approx5\%$ for the specific
lightest species and $\sim10\%$ for total baryons over total mesons at
the LHC.  The shortfall of a factor $2$--$3$ has three understood
sources, each informative rather than worrying.  \emph{Degeneracy:}
statistical yields scale as $g_i e^{-m_i/T}$, and experiment's baryon
count is lifted by the spin-degenerate octet--decuplet tower and its
feed-down; our classical quarks carry no spin, so every such factor is
absent by construction.  \emph{Kinetics versus chemistry:} statistical
hadronization \emph{assumes} chemical equilibrium at $T_{\rm ch}$,
whereas our baryon yield is set by three-body capture kinetics during
a quench --- the baryon sector falls out of chemical equilibrium first,
biasing $B/M$ low relative to an equilibrium count at the same
temperature.  This is a physical statement about recombination, shared
by coalescence approaches, not a defect.  \emph{Coupling selection:}
across the $(\Gamma,\kappa)$ plane (Fig.~\ref{fig:heatmap}) the ratio
rises monotonically to $(B{+}\bar B)/M\approx4.8\%$ at the
strongest-coupling, weakest-screening corner: demanding the measured
ratio \emph{selects} the strongly coupled regime --- precisely where
the physical quark--gluon plasma is believed to live --- turning the
discrepancy into a constraint on the model's effective coupling
rather than a failure.

The composite-penalty structure differs more sharply: experiment's
light-nuclei ladder is approximately geometric ($\sim300\times$ per
added baryon in central PbPb), whereas our quark-level ladder is
strongly non-geometric (cheap fourth constituent).  These probe
different composites (color-singlet nucleons bound at MeV scales
versus colored quarks bound at hundreds of MeV), and the lattice
framework of Sec.~\ref{sec:hierarchy} is the arbiter their difference
calls for.

\begin{table}[t]
\caption{Species ratios across the three handles.  Classical values:
$N=3000$, $H_0=0.1\,c/\fm$, $\Gamma\approx0.8$ (per-species,
normalized to mesons).  Experimental anchors are indicative
mid-rapidity LHC values; exotics carry large uncertainties.}
\label{tab:ratios}
\begin{ruledtabular}
\begin{tabular}{llll}
ratio & this work (classical) & SHM / coalescence & data anchor \\
\hline
$B/M$ & $1.5\%$ & $\sim5\%$ \cite{Andronic:2017pug} & $p/\pi^+\!\approx\!5\%$ \\
$T/M$ & $(3$--$24)\times10^{-3}$\footnotemark[1] & $10^{-4}$--$10^{-3}$ \cite{ExHIC:2017smd} &
$\chic/\psi(2S)\!\sim\!1$ (PbPb) \cite{CMS:2021znk} \\
$P/B$ & $\sim10^{-2}$ & $10^{-3}$--$10^{-2}$ \cite{ExHIC:2017smd} &
none (decay-only) \\
$5q/4q$ & $\sim0.05$ & structure-dependent & none \\
\end{tabular}
\end{ruledtabular}
\footnotetext[1]{Range spans the mean-channel and \texttt{max\_channel}
color prescriptions (Appendix~\ref{app:validation}).}
\end{table}

The empty cells of Table~\ref{tab:ratios} are the point: no prompt
pentaquark production has ever been measured --- every observed $P_c$
comes from $b$-hadron decays --- so the $P/B$ ratio from a thermal
quark medium is a genuine prediction of both halves of this work,
unconstrained by data and awaiting either enriched heavy-ion analyses
or the lattice measurement of Sec.~\ref{sec:protocols}.
