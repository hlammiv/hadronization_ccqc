# PRODUCTION: multiquark/charm-enriched corner at N = 3000, 24 seeds.
# Strong coupling + slow quench + 12 ccbar pairs: tetraquark statistics,
# pentaquark counting, and the charm-exotic (cc qbar qbar) hunt.
mode = "scan"
base = "configs/prod_test.toml"
seeds = 24
max_workers = 6
numba_threads = 2

[[points]]
name = "corner3000"
alpha_s = 0.6
h0 = 0.05
n_ccbar = 12
